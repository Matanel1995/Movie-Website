import React from 'react';


const Reviews = (movie, reviews) => {

    // const reviewItems =
    // console.log(reviews);
    //     // reviews.map((review) => {
    //     //     <p key={review.id}>review.body</p>
    // ;

    return (
        <div className='revies-Reviews'>
            {}
        </div>

    );
}

export default Reviews